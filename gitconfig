[user]
	name = Wei Liu
	email = liuw@liuw.name

[core]
	editor = vim --cmd 'set ft=gitcommit tw=78 wrap'

[color]
	ui = auto

[alias]
	st = status
	ci = commit
	co = checkout
	br = branch
	oneline = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
